{
  "name": "uuid-raid",
  "private": true,
  "scripts": {
    "dev:worker": "pnpm --filter @uuid-raid/worker dev",
    "dev:web": "pnpm --filter @uuid-raid/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  }
}